
# =============================================================================
# GENERAL CONFIGURATION
# =============================================================================

# Web package to use for the frontend
# Default: web (REQUIRED - must exist in packages/ directory)
# Used by: build scripts and server static file serving
# Example: WEB_PACKAGE=web-v2
WEB_PACKAGE=web

# Logging level for pino
LOG_LEVEL="info"

# =============================================================================
# KNOWLEDGE DATABASE CONFIGURATION
# =============================================================================

# MongoDB connection URL
# Examples:
#   Local: mongodb://localhost:27017
#   Docker: mongodb://mongo:27017
#   Atlas: mongodb+srv://username:password@cluster.mongodb.net
MONGODB_URL=mongodb://localhost:27017

# MongoDB database name
MONGODB_NAME=social-appliance

# MongoDB collection name for entities
MONGODB_COLLECTION=entities

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================

# Port for the server (Fastify)
# Default: 8080
# Used by: packages/server/src/index.ts
PORT=8080

# Host/bind address for the server
# Default: 0.0.0.0 (listens on all interfaces - required for Docker)
# Used by: packages/server/src/index.ts
HOST=0.0.0.0

# =============================================================================
# WEB INTERFACE CONFIGURATION
# =============================================================================

# JWT Secret for authentication tokens
# Generate a secure random string for production
# Used by: web interface for user authentication
JWT_SECRET=your-jwt-secret-here

# Magic Link authentication keys
# Get these from Magic Dashboard: https://dashboard.magic.link/
# Used by: web interface for passwordless authentication
VITE_MAGIC_PUBLISHABLE_KEY=pk_live_your_magic_publishable_key_here
MAGIC_SECRET_KEY=sk_live_your_magic_secret_key_here

# Mapbox access token for maps functionality
# Get this from Mapbox account: https://account.mapbox.com/access-tokens/
# Used by: web interface for location-based features
VITE_MAPBOX_ACCESS_TOKEN=pk.your_mapbox_access_token_here

# Ethereum RPC URL for blockchain integration
# Examples:
#   Local: http://localhost:8545
#   Infura: https://mainnet.infura.io/v3/YOUR_PROJECT_ID
#   Alchemy: https://eth-mainnet.alchemyapi.io/v2/YOUR_API_KEY
# Used by: web interface for Web3 functionality
VITE_ETHEREUM_RPC_URL=https://mainnet.infura.io/v3/your_project_id_here

# Google Cloud Project ID
# Get this from Google Cloud Console
# Used by: web interface for Google Cloud services integration
GCLOUD_PROJECT_ID=your-gcloud-project-id
GCLOUD_REGION=us-central1
GCLOUD_ZONE=us-central1-a
GCLOUD_SERVICE_NAME=social-appliance

# =============================================================================
# ADMIN CONFIGURATION
# =============================================================================

# NEVER EVER PUBLISH THESE ANYWHERE PUBLIC - DO NOT EMBED OR REUSE ELSEWHERE
# Admin Ethereum private key (without 0x prefix)
# Used for creating admin user and performing administrative tasks
# Generate with: openssl rand -hex 32
ADMIN_ETHEREUM_PRIVATE=your_admin_private_key_here

# Admin Ethereum public address (with 0x prefix)
# The public address corresponding to the private key above
# This address will have admin privileges and create the /admin user
ADMIN_ETHEREUM_PUBLIC=0xYourAdminPublicAddressHere

{
  "name": "social-appliance",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm --prefix packages/basic run build && npm --prefix packages/bus run build && npm --prefix packages/knowledge run build && npm --prefix packages/server run build && npm --prefix packages/web-fancy run build",
    "build:watch": "tsc --build --watch",
    "clean": "rm -rf packages/*/dist packages/*/dist-* packages/*/*.tsbuildinfo packages/*/tsconfig.tsbuildinfo node_modules/.cache",
    "dev": "npm run build && npm --prefix packages/server run server",
    "docker-build": "docker compose up --build",
    "test": "npm run build && npm --prefix packages/bus run test",
    "lint": "eslint ."
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "tsx": "^4.19.0",
    "eslint": "^9.13.0",
    "@types/node": "^22.7.4"
  }
}

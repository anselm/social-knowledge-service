import { Logger } from '@social/logger'

export class Perms {

	static write(changes,previous) {

		// - get user from a sponsor non custodial key lookup

		//const user_kid = changes.

		// - does the user own this object?
		// - does the user own parent slugs?
		// - does the object allow changes?
		// - is the user a member of a group here?

		return true
	}

}